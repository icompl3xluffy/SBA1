In Git terminal I used "mkdir" to created a folder/directory for my current project. Made it my master branch by initializing it with " git init".
Created my html file- "touch (fileName.html)" and added information.
Created new branches for my header and footer with "git checkout -b (branchName)" 
    New Branches (feature/header & feature/footer)  
Used "git branch" to see how many branches I have and to confirm which one I am in. 
Use "git checkout (branchName) navigate to each branch as needed. 
        (ex: git checkout feature/header)

To handle my merge conflict by manually checking to see what i wanted to keep in my file and deleting other stuff. After my manule edit i staged then commited the changes. 